export class Postprocessing {
    public postprocessingId: number;
    public postprocessingName: string;

    constructor(postprocessingId: number, postprocessingName: string) {
        this.postprocessingId = postprocessingId;
        this.postprocessingName = postprocessingName;
    }
}
